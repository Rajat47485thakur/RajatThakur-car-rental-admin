import { AppAlphanumericInputDirective } from './app-alphanumeric-input.directive';

describe('AppAlphanumericInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAlphanumericInputDirective();
    expect(directive).toBeTruthy();
  });
});
